<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Movies</title>
  </head>

  <body>
    <% @movies.each do |movie| %> <%= image_tag movie.image_url %> <%=
    movie.name %> <% end %>
  </body>
</html>
